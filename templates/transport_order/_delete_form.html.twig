<form method="post" action="{{ path('app_transport_order_delete', {'id': transport_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transport_order.id) }}">
    <button class="btn">Delete</button>
</form>
