{% extends 'base.html.twig' %}

{% block title %}ReflexEntries{% endblock %}

{% block body %}
    <h1>ReflexEntries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reflex_entry.id }}</td>
            </tr>
            <tr>
                <th>SocietyCode</th>
                <td>{{ reflex_entry.societyCode }}</td>
            </tr>
            <tr>
                <th>AgencyCode</th>
                <td>{{ reflex_entry.agencyCode }}</td>
            </tr>
            <tr>
                <th>ServiceCode</th>
                <td>{{ reflex_entry.serviceCode }}</td>
            </tr>
            <tr>
                <th>Operator</th>
                <td>{{ reflex_entry.operator }}</td>
            </tr>
            <tr>
                <th>Contractor</th>
                <td>{{ reflex_entry.contractor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reflex_entries_index') }}">back to list</a>

    <a href="{{ path('app_reflex_entries_edit', {'id': reflex_entry.id}) }}">edit</a>

    {{ include('reflex_entries/_delete_form.html.twig') }}
{% endblock %}
