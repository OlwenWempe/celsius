<form method="post" action="{{ path('app_reflex_entries_delete', {'id': reflex_entry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reflex_entry.id) }}">
    <button class="btn">Delete</button>
</form>
