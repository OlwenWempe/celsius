{% extends 'base.html.twig' %}

{% block title %}DonneurDOrdre{% endblock %}

{% block body %}
    <h1>DonneurDOrdre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ donneur_d_ordre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ donneur_d_ordre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_donneur_d_ordre_index') }}">back to list</a>

    <a href="{{ path('app_donneur_d_ordre_edit', {'id': donneur_d_ordre.id}) }}">edit</a>

    {{ include('donneur_d_ordre/_delete_form.html.twig') }}
{% endblock %}
