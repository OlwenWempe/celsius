{% extends 'base.html.twig' %}

{% block title %}TypeLieu{% endblock %}

{% block body %}
    <h1>TypeLieu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_lieu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_lieu.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_lieu_index') }}">back to list</a>

    <a href="{{ path('app_type_lieu_edit', {'id': type_lieu.id}) }}">edit</a>

    {{ include('type_lieu/_delete_form.html.twig') }}
{% endblock %}
