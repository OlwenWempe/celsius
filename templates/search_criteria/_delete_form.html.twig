<form method="post" action="{{ path('app_search_criteria_delete', {'id': search_criterion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ search_criterion.id) }}">
    <button class="btn">Delete</button>
</form>
